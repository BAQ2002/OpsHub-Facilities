CREATE TABLE REQUEST_TAK
(
    ID              INTEGER GENERATED BY DEFAULT AS IDENTITY,
    ID_REQUEST      INTEGER NOT NULL,
    START_DATE      TIMESTAMP,
    START_DATE      TIMESTAMP,
    DESCRIPTION     VARCHAR(200),

    CONSTRAINT PK_REQUEST_STATUS
        PRIMARY KEY (ID)
);