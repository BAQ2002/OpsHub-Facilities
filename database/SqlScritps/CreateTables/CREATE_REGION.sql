CREATE TABLE REGION
(
    ID           INTEGER GENERATED BY DEFAULT AS IDENTITY,
    ID_BUSINESS  INTEGER REFERENCES BUSINESS (ID),
    NAME         VARCHAR(100),

    CONSTRAINT PK_REGION PRIMARY KEY (ID)
);