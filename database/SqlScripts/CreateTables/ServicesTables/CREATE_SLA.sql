CREATE TABLE SLA
(
    ID          INTEGER GENERATED BY DEFAULT AS IDENTITY,
    NAME        VARCHAR(100),
    DESCRIPTION VARCHAR(255),
    DEADLINE    INTERVAL NOT NULL,

    CONSTRAINT PK_SLA
        PRIMARY KEY (ID)
);
