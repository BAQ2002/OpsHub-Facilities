CREATE TABLE SECTOR (
    ID              INTEGER GENERATED BY DEFAULT AS IDENTITY,
    NAME            VARCHAR(100) NOT NULL,
    ACCESS_LEVELS   INTEGER NOT NULL,

    CONSTRAINT PK_SECTOR PRIMARY KEY (ID)
);
